<%= form_with(model: current_user.active_followings.find_by(followed_id: @user.id),
             html: { method: :delete }, local: true) do |f| %>
  <%= f.submit "Unfollow", class: "special-button" %>
<% end %>
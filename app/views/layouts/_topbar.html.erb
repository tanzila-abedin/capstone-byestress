
            <div class="d-flex justify-content-between align-items-center topbar">
               <i class="fab fa-twitter tweet-logo"></i>    
               <div class="d-flex justify-content-between header-signout">
                    <div class="top-items"><i class="far fa-envelope"></i></div>
                    <div class="top-items"><i class="fas fa-search"></i></div>
                    <div class="top-items"><i class="fas fa-cog"></i></div>
                    <div class="top-items"><%= link_to edit_user_registration_path(@user) do%> 
                         <i class="fas fa-pen"></i>
                    <%end %></div>
                    <div class="top-items"><%= link_to "SIGNOUT", destroy_user_session_path, method: :delete%></div>
               </div>
  
            </div>
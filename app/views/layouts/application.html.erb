<!DOCTYPE html>
<html>
   <head>
      <title>Byestress</title>
      <meta name="viewport" content="width=device-width,initial-scale=1">
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>
      <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css" integrity="sha384-AYmEC3Yw5cVb3ZcuHtOA93w35dYTsvhLPVnYs9eStHfGJvOvKxVfELGroGkvsg+p" crossorigin="anonymous"/>
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
      <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
      <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
   </head>
   <body>
      <div class="container-fluid">
         <div class="row">
         <% if notice.present? %>
            <div class="notice">
            <p> <%= notice %> </p>
            </div>
            <% end %>
            <% if alert.present? %>
            <div class="alert">
            <p> <%= alert %> </p>
            </div>
            <% end %>
            <div class="col-2">
                  <% if user_signed_in? %> 
                  <div class="sidebar-container">
                     <div class="sidebar-logo">
                        <%= link_to 'Bye Stress', root_path %> 
                     </div>
                   
                        <div class="row"> 
                        <div class="d-flex justify-content-evenly align-items-center">
                        <div class="pt-2">
                         <%= image_tag "demo-pic.jpg",class:"side-img" %> 
                        </div>
                        <div>
                        <p class="grey-text"><%= current_user.fullname %></p>
                         <p class="grey-text">@<%= current_user.username %></p>
                        </div>
                        </div>
                        </div>
                        <hr>
                        <div class= "row">
                          <div class= "col text-center pt-3">
                           <% @user ||= current_user %>
                              <%= current_user.following.count %>
                               <p class="grey-text">Following</p>
                            </div>
                           <div class= "col text-center pt-3">
                              <%= current_user.followers.count %>
                              <p class="grey-text">Followers</p>
                           </div>
                        </div>
                        <hr>
                        <li>
                       <%= render 'layouts/sidebar' %></i>
                        </li>
                  </div>
            </div>
            <div class="col">
            <div class="row">
            <div class="col p-0">
            <%= render 'layouts/topbar' %>
            <% end %>
            </div>
            </div>
            <%= yield %>
            </div>
         </div>
      </div>
   </body>
</html>

